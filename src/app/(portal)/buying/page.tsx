import { CommandCenter } from "@/components/buying/CommandCenter";

export const metadata = {
  title: "Buying · Command center",
};

export default function BuyingHomePage() {
  return <CommandCenter />;
}
