import { ValueScreen } from "@/components/buying/ValueScreen";

export const metadata = {
  title: "Buying · Value realization",
};

export default function ValueRealizationPage() {
  return <ValueScreen />;
}
