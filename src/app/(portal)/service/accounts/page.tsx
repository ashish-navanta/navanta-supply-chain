import { AccountsScreen } from "@/components/service/AccountsScreen";

export const metadata = {
  title: "Service · Accounts",
};

export default function ServiceDealersPage() {
  return <AccountsScreen />;
}
