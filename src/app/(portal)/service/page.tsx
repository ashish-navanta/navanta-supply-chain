import { ServiceCommandCenter } from "@/components/service/ServiceCommandCenter";

export const metadata = {
  title: "Service · Command center",
};

export default function ServiceHomePage() {
  return <ServiceCommandCenter />;
}
